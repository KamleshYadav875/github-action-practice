# Workflow

# Should have name
name: Hello

# On tells when the workflow should run
on: 
  workflow_dispatch: 

# Jobs
# Should be inside the workflow
# jobs tells what the workflow should do
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hellp to everyone
        run: echo "Hello Dosto"
